namespace MarketplaceApi.Helpers
{
    public class CoinsSettings
    {
        /// <summary>Flat coins awarded each time a user submits a store review.</summary>
        public decimal ReviewCoins { get; set; } = 20;
    }
}
